import React from "react";
import { BLOCK_SIZE } from "../globals";

export const Sky = () => (
    <div
        style={{
            width: "100%",
            height: "100%",
            background: "transparent"
        }}
    />
)